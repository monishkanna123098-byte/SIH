# Everything is pip-installable and nothing is fetched at runtime: no CDN
# fonts, no CDN CSS, no icon library. The app runs with networking disabled.
#
# Web application (chunk 4)
fastapi
uvicorn
jinja2
python-multipart
# Reports -- lm_report imports these lazily, inside render_pdf/render_docx
reportlab
python-docx
# Measurement engine and the integration regression test
numpy
scipy
opencv-python-headless
pillow
# Test client for test_webapp.py only
httpx

# ---- OPTIONAL -------------------------------------------------------------
# Everything below is optional. With all of it absent the app starts, runs and
# passes every test; automated extraction is simply absent and the manual path
# -- a first-class input, not a fallback -- is unchanged.
#
# Automated declaration extraction (chunk 8). Needs LM_VISION_API_KEY set too;
# with no key the button does not render. Install whichever provider the team
# has a key for -- LM_VISION_PROVIDER selects it, and each SDK is imported
# lazily, so neither needs to be present for the app to start.
anthropic
google-genai
# The OCR cross-check. Also needs the `tesseract` binary on PATH
# (Debian/Ubuntu: apt-get install tesseract-ocr). Absent -> the cross-check is
# skipped entirely and every field stays verbatim_confirmed=None.
pytesseract
